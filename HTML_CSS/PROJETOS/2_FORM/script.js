mudaValor()

    function mudaValor() {
        minHigh.innerText = idMinHigh.value + 'm';
        maxHigh.innerText = idMaxHigh.value + 'm';
        maxDistance.innerText = idMaxDistance.value + 'km';
        maxAge.innerText = idMaxAge.value + ' anos';
        minAge.innerText = idMinAge.value + ' anos';
    }